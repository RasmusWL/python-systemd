---
# vi: ts=2 sw=2 et:
# SPDX-License-Identifier: LGPL-2.1-or-later
#
name: "CodeQL"

on:
  push:
    branches:
      - rasmuswl
  pull_request:
    branches:
      - rasmuswl

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-22.04
    concurrency:
      group: ${{ github.workflow }}-${{ matrix.language }}-${{ github.ref }}
      cancel-in-progress: true
    permissions:
      actions: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: ['python']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          sudo apt -y update
          sudo apt -y install gcc libsystemd-dev

      - name: debug
        run: |
          python3 --version

          python3 -m pip install --user --upgrade pip setuptools wheel
          python3 -m pip install --user 'virtualenv<20.11'
          python3 -m pip install --user 'poetry>=1.1,<1.2'
          python3 -m pip install --user pipenv

          python3 -m virtualenv /home/runner/work/python-systemd/wat-debug
          ls /home/runner/work/python-systemd/wat-debug

          cd /home/runner/
          python3 -m virtualenv /home/runner/work/python-systemd/wat-debug2

          ls /home/runner/work/python-systemd/wat-debug2/
          exit 1
        shell: bash -e -x {0}

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v2
        with:
          languages: ${{ matrix.language }}
          queries: +security-extended,security-and-quality

      - name: Autobuild
        uses: github/codeql-action/autobuild@v2

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v2
