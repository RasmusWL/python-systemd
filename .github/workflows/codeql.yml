---
# vi: ts=2 sw=2 et:
# SPDX-License-Identifier: LGPL-2.1-or-later
#
name: "CodeQL"

on:
  push:
    branches:
      - rasmuswl
  pull_request:
    branches:
      - rasmuswl

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-22.04
    concurrency:
      group: ${{ github.workflow }}-${{ matrix.language }}-${{ github.ref }}
      cancel-in-progress: true
    permissions:
      actions: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: ['python']

    steps:
      - name: debug
        run: |
          python3 --version

          # python3 -m pip install --user --upgrade pip setuptools wheel
          python3 -m pip install --user --upgrade pip wheel
          python3 -m pip install --user 'virtualenv<20.11'
          # python3 -m pip install --user 'poetry>=1.1,<1.2'
          # python3 -m pip install --user pipenv

          python3 -m virtualenv /home/runner/work/python-systemd/wat-debug
          ls /home/runner/work/python-systemd/wat-debug
          ls -R /home/runner/work/python-systemd/wat-debug/lib
          cat /home/runner/work/python-systemd/wat-debug/pyvenv.cfg

          cd /home/runner/
          python3 -m virtualenv /home/runner/work/python-systemd/wat-debug2

          ls /home/runner/work/python-systemd/wat-debug2/
          /home/runner/work/python-systemd/wat-debug2/bin/pip --version
        shell: bash -e -x {0}
